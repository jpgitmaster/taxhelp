const initStatus = {
    message: '',
    loader: false,
}

const initFilter = {
    search: '',
    // filter: { roleId: ['2'] },
    filter: { roleId: [] },
    currentPage: 1,
    recordsLimit: 10
}
export {
    initStatus,
    initFilter
};